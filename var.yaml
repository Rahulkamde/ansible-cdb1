- name: variable playbook 
  hosts: all 
  vars: 
    url: www.global-var.com
  
  tasks: 
    - name: task1 print 
      debug: 
        msg: "{{url}}"
      vars: 
        url: www.local-var.com

    - name: task2 print
      debug:
        msg: "{{url}}"
        